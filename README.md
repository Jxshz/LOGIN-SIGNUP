Day 1 of creating websites using vibe coding 

With my style and adding it to the database (Supabase) which i used to handle the data because it uses the PostgreSQL.
Today i created a login with sign up page which uses the triggers feature from the Supabase for sign up (new users) meanwhile the login feature uses the authentication by manually by me.

Tech Stack :
 - HTML
 - CSS3
 - Javascript
 - Supabase

